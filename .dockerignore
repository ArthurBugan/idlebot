# Build cache bloat
target/
**/target/
dist/
**/dist/

# VCS / misc
.git/
.gitignore
*.md
.env
.env.*
.DS_Store

# Unrelated toolchains (not workspace members)
# NOTE: contracts/ is needed by Dockerfile.solana, so it is no longer
# excluded here; its build artifacts are still excluded via **/target/.
node_modules/

# Local cargo config overrides
.cargo/
